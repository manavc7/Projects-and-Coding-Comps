#user inputs mass as an integer (in kilograms) and program outputs the equivalent number of Joules as an integer assuming that the user will input an integer.
m = input("m: ")
print("E = " + str(int(m) * (300000000**2)))
